import 'package:flutter/material.dart';

class AnimationAndMotionWidget extends StatelessWidget {
  const AnimationAndMotionWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('애니메이션'),
        centerTitle: true,
      ),
    );
  }
}