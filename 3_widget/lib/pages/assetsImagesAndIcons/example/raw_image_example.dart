import 'package:flutter/material.dart';

class RawImageExample extends StatelessWidget {
  const RawImageExample({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('RawImageExample'),
        centerTitle: true,
      ),
      body: const Placeholder(),
    );
  }
}
