import 'package:flutter/material.dart';

class InteractionModelsWidget extends StatelessWidget {
  const InteractionModelsWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('상호작용'),
        centerTitle: true,
      ),
    );
  }
}